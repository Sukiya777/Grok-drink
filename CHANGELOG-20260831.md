# 变更说明 · 2026-09-02（二次打开秒进：缓存 + 色veil + 倒酒开屏）

## 同日补丁：倒酒液体贴合杯壁梯形

你实测发现：岩石杯杯壁是由窄变宽的梯形，但液体始终是个长方形。根因是我
裁剪液面用的是矩形 clipPath，和杯壁线条成了"两套几何各画各的"。
修复：裁剪区改为沿杯壁内缩 3 单位的梯形（底部小圆角），并把杯壁、裁剪区、
液面高光全部改成同一组常量（RIM/FOOT/BASE_T/WALL/TOP_Y）推导 —— 单一事实
来源，之后改杯型只动常量。位移逻辑不变（仍是 transform，兼容旧 Safari），
液面到哪一级就自然收到那一级杯壁的宽度。
质检：jsdom 抓组件真实 SVG 输出 8 帧（0/3/6/9/12/20/35/51 张已加载）→
rsvg 栅格化 → 逐行比对液体左右缘与杯壁理论轮廓，全部贴合（±3.5px）；
空杯帧液体像素精确为 0。帧板见 夜酌-倒酒液位适配检查.png。
（踩坑记录一：首版检测器把 #e8ddd0 口沿线误判为液体，报 38 处越界；
液体判据须用 r−g>26 把线色挡掉——线色 r−g 只有 11。
踩坑记录二：梯形左右边界首次实现时右侧内缩符号写反（+WALL 应为 −WALL），
被像素仲裁以"右缘恒外凸 6 单位"当场抓住——这就是先跑数再下结论的价值。
踩坑记录三：孤图评审两次判"液体仍是直边"，像素采样证明左缘 115→145px 斜率
与杯壁理论值吻合（0.156 vs 0.1525），放大并列复审确认贴壁——微差在缩略图
上肉眼不可分，评审结论必须经像素仲裁，维持既有工作流。）

这轮解决一个真实体验问题：冷启动时列表先是一片黑底，插画再一张张刷出来。
根因不是图太大（51 张共 1.16MB，平均 23KB），而是①卡片图开了 loading="lazy"
②项目没有任何缓存手段，每次打开都要重新下载。所以三招一起上：

**C · PWA 离线缓存（治本）**
- public/sw.js：Service Worker，install 阶段预缓存壳资源 + 全部 51 张插画；
  页面走 network-first（改版即时可见，断网回退缓存壳），静态资源与 Google Fonts
  走 stale-while-revalidate（先给旧的、后台补新的）。
- public/manifest.webmanifest + public/icons/（192/512/maskable/apple-touch 四件，
  由 scripts/gen-pwa-icons.py 从既有 favicon 几何直接画，与品牌形一致）。
- src/main.tsx 注册带 https + 能力守卫：file:// 预览、老内核一律静默跳过。
- 效果：第二次打开起，图片全部命中缓存，滚动不再出现"慢慢刷新"。
  ⚠️ 以后改插画或改壳，需把 sw.js 里 CACHE 的 night-pour-v1 尾号 +1 才能整体换血。

**B · 色veil 渐进显影（治观感）**
- src/lib/art-veil.ts：51 杯酒液主色表，由 scripts/gen-art-veil.py 从成品图反推
  （HSV 饱和度≥0.18 筛掉米白墙与木桌，按色相分桶取最大有色面积=液体，
  均值向产品墨色收 15%）。收暗是故意的：占位色不能比成品更跳，否则淡入时是占位在发光。
- src/components/art-img.tsx：容器铺酒色、图片解码完再淡入（--motion-slow）。
  加载中的占位是一块"这杯酒的颜色"而不是黑底——黑暗中先看见酒色，再看清杯子。
  接入列表卡（前 8 张 priority 不懒加载）与详情页横幅；淡入只加在 img 上，
  容器负责底色，否则占位色会跟图一起透明掉。

**D · 倒酒进度开屏（把等待变成有用的一秒）**
- src/components/brand-splash.tsx 重写：岩石杯线条，杯中液位 = 51 张插画的真实
  预加载进度（60% 权重给前 12 张、40% 给全量，避免"迟迟不涨最后猛满杯"）。
  封顶 2.6s 必退场，点按任意处跳过，每会话只播一次，reduced-motion 不播。
- 液位动画用 translateY + 外层 clipPath 双层结构：SVG 几何属性（y/height）的
  CSS transition 在旧 Safari 不可靠，且 transform 若与 clipPath 同层会把裁剪区一起推走。
- ⚠️ 沿革：上一版的"笔顺运笔写夜酌"已整体退役（删 brand-strokes.ts 与 splash-stroke 样式）。
  原因量化后确认无法靠调参救：骨架是 6px 圆头单线（墨量 1.7%），成品是衬线字
  （横 2.5/竖 12.2、4.9:1 粗细对比 + 装饰角，墨量 22.7%），中心线 IoU 仅 0.04，
  用骨架去擦显衬线字最多盖到 8%~16% 的墨——"细线写完换字体"这个结构本身就不成立。
  倒酒与品类同语言，且顺带把加载进度这件事说了。

验证：tsc / eslint / verify（51 杯数据自检）/ build 全绿；jsdom 六套回归共 61 项通过，
其中 splash_smoke 重写为倒酒版 14 项（首开渲染、点按跳过、封顶退场、二开不播、
reduce 不播、veil 在场且色值合法不雷同）。9 款 veil 色的评审质疑经像素仲裁，
8 款判误杀（取色确在液体核心区），1 款看图复核为背景深蓝色的咖啡油脂泡沫，均不改。

---

# 变更说明 · 2026-09-02（开屏运笔动画：「夜酌」逐笔写出）

> ⚠️ 本条目已于同日第二轮整体退役（原因见文末；brand-strokes.ts 与 splash-stroke
> 样式已删除，开屏改为倒酒进度）。保留此段是记录"为什么不成立"，别再走回头路。

灵感来自 X/推特开屏：不做 logo 冲场，做"运笔写品牌名"，和水墨插画同一套语言。

实现（零新增依赖、零运行时请求）：
- src/lib/brand-strokes.ts 内置「夜/酌」笔顺骨架，取自 hanzi-writer-data
  （MIT，Chinese-Character-Stroke 笔顺库）的 medians 中心线，Catmull-Rom 平滑
  并预计算弧长。夜 8 笔、酌 10 笔，与国标笔画数一致。
- src/components/brand-splash.tsx：每笔用 stroke-dasharray/dashoffset + CSS
  keyframes 书写，时长按弧长分配（SPEED 5.5 单位/ms、下限 60ms），点与撇捺
  因此有 60~165ms 的长短呼吸，不是等长打印感。写完 260ms 与正式衬线字交叉淡入
  （NIGHT POUR、CRAFTED BY SUKI 各错峰 180ms），整层 3.77s 播完卸载。
- 每会话只播一次（sessionStorage），点按任意处即跳过；
  prefers-reduced-motion 下组件直接不渲染，CSS 另有 dashoffset:0 兜底防空白。

关键度量决策：两字不再共用 viewBox。旧版夜墨迹占 91.7%、酌占 80.4%，同框渲染
「酌」会小一圈且夜的底边超出 25 单位被裁。现每字按墨 bbox 独立取景，使
「骨架含描边墨长边 = 衬线字墨长边 83px / 104px 盒」，两字等大、叠化不跳字
（实测骨架 84x82 / 82x83 vs 衬线 83x83 / 83x82）。

质检：写抽帧脚本按真实时间轴与真实布局离线渲染 9 帧核对。审稿曾判"酌是错字、
驳回"，经像素仲裁推翻——权威轮廓与中心线同 y 坐标系下，夜/酌中心线落笔率均
100.00%，平滑后渲染路径亦 100.00%（其间两次 9.7%/22.5% 均为脚本自身的坐标系
bug，非产品问题）。新增 jsdom 第 6 套 splash_smoke（15 项：首开 18 笔、二次
不播、reduce 跳过、播完卸载、点按跳过、标记写入），全量 62 项回归通过。

---

# 变更说明 · 2026-09-02（品牌区三行间距对齐 + 顶部留白 + 刘海适配）

用户反馈：夜酌 / NIGHT POUR / CRAFTED BY SUKI 三行间距不一致，且夜酌顶得太靠上。
属实且是我的两处失误：① 加署名行时直接套了默认档位 mt-1 / mt-2，没按光学间距算；
② 夜酌用了 leading-none（行高 24px），而 Noto Serif SC 24px 的自然字身是 35px，
行高小于字身会使墨迹向上溢出行盒 5.5px——padding 名义 16px，实际顶部只剩 17.5px，
所以看着"顶"。

修法不是调 margin，而是按字体度量重排行高。用 PIL 取三行的自然字身与 bbox，
以「光学间隙 = 下一行墨顶 − 上一行墨底」为目标函数，在 Tailwind 可表达的档位上
穷举，选定零溢出且间隙严格相等的一组：pt 20px，三行行高 36 / 20 / 18，margin 全归 0。
光学间隙由 6.5 / 15.0px（差 8.5）变为 10.5 / 10.5px（差 0），夜酌墨迹距顶 17.5 → 27.5px。

同时补上真实缺陷：项目原本只有 .pb-safe 做底部安全区，品牌区是移动端页面最顶元素，
加到主屏全屏时会被刘海压住。按同一惯例新增 .pt-safe（20px + env(safe-area-inset-top)，
≥768px 回退固定 20px），index.html 已带 viewport-fit=cover，故直接生效。
这里用字面量 20px 而非 --space-5：项目的 --space-5 是 24px，会把顶部推过头。

配套修好单文件预览脚本的两个自身 bug：① String.replace 的替换串未用函数形式，
bundle 里 React 的 c.replace(re,"$&/") 被 "$&" 展开成脚本标签，污染了代码；
② 该项目的 CSS <link> 排在 <script> 之后，跨标签 .*? 匹配会把 script 吞进 <style> 块。
现方案为：bundle 不含 ESM 语法，摘掉 head 内的模块脚本、注入经典脚本到 </body> 前。
五套 jsdom 回归 47/47 全绿（12+9+11+9+6）。

另：51 张插画复检时 french-75 / gin-tonic / moscow-mule 四角背景极差 13/15/49 超阈值，
与无水印母本比对确认是母本固有的水彩纸纹渐变（母本 13/18/58，加水印后反而略降），
非水印或白边问题，不重做。

---

# 变更说明 · 2026-09-02（作者署名：品牌落款 + 插画水印 · 51 款）

侧栏品牌区新增第三行落款 `夜酌 / NIGHT POUR / CRAFTED BY SUKI`（10px、字距
0.22em、text-subtle），前两行排版不动，形成"标题—副标—落款"三级层级。
否决了"NIGHT POUR for Suki"同行后缀：语义上 for 会被读成"献给"，且大小写混排
会破坏 NIGHT POUR 全大写宽字距撑出的干净感。

51 张插画统一加右下角轻水印 `SUKI`：衬线体（Noto Serif SC，与产品 display 同源）、
17px、距边 44px，颜色取该图局部背景色向白插值 20%，即"比背景亮一档"而非硬白。
距边 44px 是刻意设计——避开五道机检的边缘/四角采样带，加水印后 51 张的白边、
四角背景、残留色带三项复检仍全绿。

配套保全：无水印母本已单独归档（仓库外 Grok-drink-art-master-20260902.zip，51 张），
脚本入库 scripts/watermark-art.py（支持目录/单文件/--alpha/--dry），已验证重跑输出
与线上像素完全一致，故水印风格随时可调可撤。新增单款酒时对单个文件跑一次即可。

顺带修复测试脚本自身的脆弱判据：featured_scroll 硬编码匹配"全部 50"，新增第 51 杯
后点不到分类按钮导致 3 项误判 FAIL。已改为按"以数字结尾"匹配，与杯数解耦。

---

# 变更说明 · 2026-09-02（新增「落日椰林」+ 放大提示改居中 · 总 51 款）

用户在抖音看到「落日椰林」，库中原无此款（仅有朗姆基的椰林飘香，配方无关），
按截图配方新增：金酒 50 + 君度橙酒 15 + 红石榴糖浆 10 + 椰奶 90，shake 满方冰，
干橙轮装饰，归入金酒系（金酒 9 款）。材料表新增「君度橙酒」，并把「椰奶」
加进同义归一表映射到已有的「椰浆」，避免可勾选材料重复膨胀。

插画 v1 被审稿判两项：液体偏透明、"杯子外缘粗白描边"。v2 用正向描述
（酒液=一整片不透明乳浊粉橘、冰块只露在杯口之上）解决透明度，并经与已定稿
椰林飘香并列对照确认达标。白描边一项经三重证据判定为审稿误判：并列对照实验
回答"一致"；像素级仲裁显示紧贴背景的第一圈高亮占比 50.7%，与已认可的椰林飘香
47.3%、椰菠萝 40.5% 同量级（真白描边会接近 100%，暗液体的皇家基尔仅 6.5%）。
教训固化：孤图评审会把"奶白液体本身偏亮"误读为白描边，风格类判定必须做对照实验。

UI：详情页插画下方的「点击看大图」提示由右对齐改为图片下居中。

---

# 变更说明 · 2026-09-02（★全 50 款插画收官 · 50/50 + 早期批次边缘修复）

无酒精系 5 款齐图收官：处女莫吉托、雪莉谭宝、柑橘高球、黄瓜通宁、椰菠萝。
收官款椰菠萝为全库唯一飓风杯：v1 撞上"杯脚实心暗块"历史红线（3/10），v2 改用
正向描述（杯脚=两条竖直细墨线、中间直接透出背景；底座=细墨线椭圆环、环内与
背景同色中空）后六项全 10/10 采纳。

同时做了一次全库边缘体检，查出早期 9 张（杏仁酸酒、美式、阿佩罗气泡、自由古巴、
莫斯科骡子、帕洛玛、斯巴克良、龙舌兰日出、汤米玛格丽特）最外圈残留 1~4px 米色
细线（早期换背景留下的抗锯齿边，四角检测因采样点内缩 15px 而漏检）。以
"中心区域字节原样保留、仅四边贴 4px 纯背景色带"的无损方式刷除，逐张验证
"内部原样=True"，全库 50 张现通过白边 / 四角背景 / 边缘残留带三项终检。

新增第五道关卡：ring_diag 边缘残留带检测（逐边向内探连续亮带像素数）。

进度：威士忌 8 + 金酒 8 + 朗姆 7 + 龙舌兰 6 + 伏特加 6 + 干邑白兰地 5
+ 利口开胃 5 + 无酒精 5 = **50/50 八大类目全部集齐**。

---

# 变更说明 · 2026-09-02（龙舌兰分类插画收官 · 总 45/50）

牧场水一发 60/60 满分（淡粉葡萄柚blush清透酒液一次画准）。恶魔 v1 内容全对
但杯子外缘又现粗白勾边（第4例，同款病），v2 把"白线禁令"升级为"想象细墨线
是杯子与背景之间的唯一分界"后彻底清除，采纳。龙舌兰系 6 款全部有图（玛格丽特、
帕洛玛、汤米玛格丽特、龙舌兰日出、牧场水、恶魔）。白线勾边累计退回 4 例，
该禁令已成为标准提示词模板的一部分。

---

# 变更说明 · 2026-09-02（干邑/白兰地批次插画收官 · 总 43/50）

皇家基尔（本批收官款）：v1 白线勾边+实心白杯脚+纯白泡沫三病并发（轮廓项仅
3/10）退回；v2 把三条修复意见逐条写成禁令，三病全清采纳。至此干邑系批次
5 款齐图：边车、白兰地亚历山大、皮斯科酸、纸间、皇家基尔。累计沉淀的三条
系列禁令：外轮廓禁白线（贴纸感）、杯脚禁实心块（必须透出背景）、装饰数量
写死。

---

# 变更说明 · 2026-09-02（皮斯科酸 + 纸间插画 · 总 42/50）

皮斯科酸一发过审（蛋白泡沫盖约1/3杯身高、恰好3滴苦精点）。纸间三发：v1 白线
勾轮廓（同金汤力病）退回；v2 墨线修好但整体偏右；v3 未再烧生成，用程序化平移
（内容包围盒中心对齐画布中心，-53px/+6px）修正构图，复检无裁切后采纳。新增
工具 /tmp/20260902_recenter.py：构图偏心类问题优先平移修复，不浪费生成额度。

---

# 变更说明 · 2026-09-02（朗姆分类插画收官 · 总 38/50）

戴吉利（一发 59/60，乳白雾面质感 + 绿青柠轮均准）、椰林飘香（v1 因杯脚投影
+背景金星退回，v2 全净采纳）、迈泰（一发 59/60，青柠壳+薄荷枝双装饰还原）、
黑暗风暴（一发过审，黑朗姆压金姜汁的"暴风雨"两色分层画出来了）。朗姆基酒
7 款全部有图（莫吉托/戴吉利/椰林飘香/黑暗风暴/迈泰/自由古巴/海明威特调）；
双基酒的纸间归入白兰地批次的干邑系。装配阶段延续四道质检 + 先 grep 登记表
再补行的流程，登记↔文件 38/38 对齐。

---

# 变更说明 · 2026-09-02（伏特加分类插画收官 · 总 34/50）

浓缩马天尼（一发过审：crema 分层 + 恰好三颗咖啡豆）、血腥玛丽（v1 内容满分
但金线勾边退回，v2 改深墨线主轮廓采纳；芹菜茎纵棱、黄柠檬轮均准确）、
伏特加马天尼（吸取干马天尼器型经验，提示词写死"碗深≈杯口宽80%"，一发命中
59/60；杯周小金星点缀按新规判定不违和，予以保留）。至此伏特加系 6 款全部
有图。质检四道关卡（内容审稿 / 四角背景一致性 / 白边 / 登记↔文件一致）
每张全过。

---

# 变更说明 · 2026-09-01（蜂刺背景分层返工 · 新增第四道质检关卡）

用户肉眼发现蜂刺 v1 背景"多层"违和——量化证实：四角（纯背景区）亮度
16/21/30/41，跨度 24.8，即背景存在画框感/晕影。v2 修平背景（跨度 0.7）但
杯侧有残次线条；v3 在提示词中加入"洁净度禁令"（无杂线、无杯脚投影、背景
四角一致）后达标：四角 29/29/29/29、无白边、视觉审稿 9.4/10，采纳。
返工后对全库 31 张做背景四角复检：除已替换的蜂刺外无异常。
教训：视觉模型对"背景分层"漏检（v1 曾给它 60/60），此后每张新图在内容审稿、
白边自检、登记一致性之外，新增【四角背景一致性 ≤12】程序化硬检。

---

# 变更说明 · 2026-09-01（金酒分类插画收官 · 总 31/50）

按"一轮一款、逐张审稿"节奏补齐金酒分类 5 张：金汤力（首版 Seedream 白描边
风格不符退回，Gemini 重抽 9/10 采纳）、航空（v1 酒液偏绿，v2 淡紫蓝 10/10）、
法式75（60/60）、最后的话（60/60）、蜂刺（60/60）。每张均过三道关卡：
内容对照配方字段审稿、边缘亮度白边自检、登记表↔文件一致性校验（该自检
当场逮住两次登记缺行）。金酒基酒 8 款（含干马天尼、内格罗尼、汤姆柯林斯）
至此全部有图。

---

# 变更说明 · 2026-09-01（金酒系插画集齐 8/8）

金酒分类全部配上插画（总 31/50）：新增金汤力、航空、法式75、最后的话、
蜂刺 5 张，逐张通过"杯型/颜色/装饰白名单/无文字"机器核对：

- 航空：淡薰衣草蓝酒液 + 绿樱桃特征完美还原（满分通过）
- 最后的话：首版樱桃画成黑紫色（丢了吧里的灵魂绿樱桃），重抽 v2 采纳
- 法式75：笛杯/金泡全对，装饰色偏棕（小瑕疵，可接受）
- 金汤力：直筒杯满方冰气泡感十足，多了两颗杜松果（酒谱本就允许，采纳）
- 蜂刺：蜜金酒液 + 柠檬皮卷，满分通过

---

# 变更说明 · 2026-09-01（干马天尼插画 · 三选一审稿）

金酒系首张插画 martini 上线（总 26/50）。生成 3 版候选：v1 柠檬皮卷质感好但
碗画深（视觉评审判为"深碗杯"）；v3 被判画成 coupe 且构图偏小（6 分）；
v2 除"碗略浅于标准钟形"外各项全对、8.5 分，采纳 v2。边缘亮度自检无白边。

---

# 变更说明 · 2026-09-01（今日特调改为随列表滚动）

「全部」页的今日特调卡片原先钉在滚动区上方，下拉时永久置顶——推荐位常驻
不符合"头条内容"的产品直觉，且常驻吃掉手机一屏约 1/6。现把卡片移入
#cocktail-scroll 作为第一块内容，往下滑正常滚走；仅在「全部」且无搜索词
时出现的行为不变，点击跳转逻辑不变。

详情页插画注脚由静态"成品示意"改为可点的「🔍 点击看大图」按钮（放大镜
图标 + 文案，右对齐贴图缘），提示放大交互；点图本体同样可放大。

---

# 变更说明 · 2026-09-01（酒款插画 · 扩量至 25 款）

第二批 21 款插画上线（四宫格批量生成 + 逐格机器核对），覆盖：
威士忌系全部 8 款（曼哈顿/威士忌酸/萨泽拉克/青霉素/林荫大道/纽约酸/
老广场/白俄罗斯）、特基拉系 5 款（玛格丽特/汤米/帕洛玛/龙舌兰日出/
海明威）、开胃利口 4 款（阿佩罗橙光/美式/斯巴留加图/杏仁酸酒）、
及托姆柯林斯、薄荷朱利普、莫斯科骡子、自由古巴。

1. 图片统一 800×800 WebP（平均 26 KB，最大 52 KB），`public/images/cocktails/`。
2. 登记扩到 25 款，其余 25 款继续显示线条杯型图，后续分批补齐。
3. 每款图形按酒谱字段绘制：杯型/冰型/装饰白名单/酒液颜色，
   重点易错项（萨泽拉克绝对干无冰、朱利普金属杯、骡子铜杯、日出渐变）已逐张核对。
4. **白边修复**：6 张图（威士忌酸/纽约酸/白俄罗斯/老广场/青霉素/莫斯科骡子）
   生成时自带白色背景边，已按"暗色内容包围盒裁切→补方→重压缩"程序化修复，
   25 张边缘亮度全表复核通过。后续批次生成流水线加入同款自检。

---

# 变更说明 · 2026-09-01（酒款插画 · 首批 4 款试点）

AI 手绘风成品插画上线（路线 A：墨线 + 低饱和水彩 + 深墨蓝底），先做 4 款
代表杯型验证效果：古典鸡尾酒 / 内格罗尼 / 大都会 / 莫吉托。

1. 图片在 `public/images/cocktails/*.webp`（480×480，单张 12~16 KB），
   新增 `src/lib/drink-art.ts` 登记 id → 路径映射。
2. **列表卡片**：有插画的酒在卡片右侧显示 64px 小方图（在点击区内，点图
   也能进详情），懒加载；没插画的酒保持原样，布局不受影响。
3. **详情页**：头部右侧 112~144px 大图替换线条杯型示意，配"成品示意"
   注记；完成步骤的"倒酒"动画在大图上以提亮放大呈现（art-pour），
   无图酒款沿用原 glass-pour。
4. 后续批量补齐 50 款时，只需生成 webp + 在 drink-art.ts 加一行。
5. **点图放大（lightbox）**：列表小方图与详情页大图均可点击弹出全屏大图
   （`art-lightbox.tsx` + `lib/art-view.ts` 发布/订阅，无新依赖）。点任意
   遮罩 / 右上 ✕ / Esc 关闭；卡片上点图不冒泡进详情；图片升级 800px
   （单张 35~48 KB，仍走懒加载）。弹入动画纳入 reduced-motion 降级名单。

---

# 变更说明 · 2026-09-02（互动反馈升级）

按"每个动作都有回声"原则补齐操作反馈（克制档，不动版式）：

1. **Toast 小纸条**（`src/lib/toast.ts` + `src/components/toaster.tsx`）：
   复制配方/链接、完成全部步骤时底部浮出提示，1.8s 自动消失，aria-live 播报。
2. **调酒进度线**：配方页顶栏下沿细线随已完成步数生长；勾满全部步骤触发
   "倒酒"动画（酒杯液面上涨加深）+ toast「这杯成了」+ 双击震动。
3. **触感反馈**（`src/lib/haptics.ts`）：收藏、勾选材料、完成步骤、复制成功
   时 navigator.vibrate 轻震；桌面/iOS Safari 静默跳过，不报错。
4. 列表心形补 heart-pop（原来只有配方页有）；吧台勾选对勾加 check-pop 弹性。
5. 新动画全部纳入 prefers-reduced-motion 降级名单。

---

# 变更说明 · 2026-09-02（手机使用反馈修复）

1. **我的吧台整页滑不动**：材料面板和酒单分属两个区域，52 个标签把酒单挤出
   视口，两边都滚不动。现把面板整体移入主滚动区，材料区 → 分组酒单成为
   一条连续滚动流。第一版"只给标签列表加内部滚动"的方案已回退——
   同屏两个滚动区，用户很难感知（感谢反馈指出）。
2. **苦精永远算缺口（逻辑自相矛盾）**：苦精类被排除在可勾选材料之外，
   却仍计入"还缺几样"，导致古典鸡尾酒、曼哈顿等永远进不了"现在就能调"。
   `matchByPantry` 现在与 `PANTRY_OPTIONS` 同源：苦精/调味料不计缺口。
   `verify-data.mjs` 增补回归断言。
3. 勾 1 个基酒看不到酒属正常（默认"最多缺 1"，多数经典配方需 2~3 样材料），
   工具栏"最多缺 0-3"可放宽，空列表提示已有引导文案，未再改动。

验证：数据自检 / tsc / eslint / 构建全绿；jsdom 吧台交互专项 11/11 通过。

---

# 变更说明 · 2026-08-31

对 `Sukiya777/Grok-drink` 的一次重构，两条主线：**工程瘦身** 与 **体验/视觉优化**。

## 一、工程瘦身

原仓库只有一个 `Grok-drink.zip`，解压后是 Grok App Builder 生成的 TanStack Start 全栈脚手架。
承载这个调酒站的代码是 1,427 行酒谱数据加约 1,170 行界面与样式，其余都是模板自带的、
本项目根本不会执行的部分。

### 删除

| 内容 | 说明 |
| --- | --- |
| `src/lib/auth/`（13 个文件，2,057 行） | better-auth 登录体系：邮箱密码、OAuth popup、session gate、PG 隔离 |
| `src/lib/app-data/`（6 个文件，910 行） | Google Drive / Gmail / Outlook / MCP 连接器调用层 |
| `src/lib/multiplayer/`（579 行） | WebRTC 多人联机，无任何入口调用 |
| `src/lib/db.ts`（238 行）、`migrations/`（67 行） | PGLite + Kysely + Postgres 与建表 SQL |
| `src/lib/preview-host-bridge.ts` 等（365 行） | Grok 沙箱预览 postMessage 桥、embedder 白名单 |
| `src/routes/`、`src/router.tsx`、`routeTree.gen.ts`（131 行） | TanStack Router/Start 路由层，改为单入口挂载 |
| `server/`（124 行）、`scripts/`（4,900 行） | Grok PWA 安装页、品牌校验、浏览器冒烟、预览守护、签名计划 |
| `public/__grok/`（990 行）、`.grok/`、`startup.sh` | Grok 平台安装引导与容器启动脚本 |

合计约 10,400 行死代码与死资源。

### 依赖 70 → 22 个（运行时 51 → 5）

保留 `react` / `react-dom` / `lucide-react` / `clsx` / `tailwind-merge`。
移除全部 21 个 Radix 包、TanStack 全家桶（router/start/query/table）、better-auth、pglite、pg、
kysely、jose、recharts、react-hook-form、zod、zustand、date-fns、cmdk、vaul、sonner、
`react-day-picker`、`tw-animate-css`、`class-variance-authority`（Button 的 cva 已手写为变体表）；
构建期只留 vite / tailwind 4 / eslint / prettier / typescript 所需最小集，移除 `nitro`、`playwright`。

### 构建方式

`@tanstack/react-start` + `nitro`（Vercel 函数）→ 纯 `vite build` 静态产物。
现在冷构建约 0.5 秒（原方案在此环境未能完成依赖安装，故不做耗时对比）；
产物 `dist/` 直接可托管，无 SSR、无服务端运行时。

`vite.config.ts` 183 行 → 16 行：删除 dev 阶段的 OAuth popup 中间件、PGlite 引导钩子、
thumbnail/OG 注入与 `/__grok/install` 路由。
`package.json` 的 `build` 改为 `tsc --noEmit && vite build`，类型不过就不出包；
`db:migrate`、`check:auth`、`preview:*` 等脚本随之移除。

> 原 README 写着「本项目不需要数据库和登录」，但工程里确实带着完整的登录与数据库层，
> 且 `npm run build` 会顺带跑一次 `db:migrate`。业务代码从未 import 它们
> （唯一引用是 `__root.tsx` 里一个直接 return children 的透传 `AuthProvider`），所以移除是安全的。

## 二、顺手修掉的问题

逐项说明。其中第 3 条是我改动过程中自己引入、被冒烟测试捕获的，不是原仓库的缺陷。

1. **搜索时整列表卸载重建**：原 `CocktailList` 挂着 `key={rail + query}`，每敲一个字 React 就把
   全部卡片销毁重建，配合未做延迟的搜索输入，掉帧明显。已去掉该 key，改用
   `useDeferredValue` + 稳定 key。
2. **复制配方在非安全上下文静默失败**：原实现只走 `navigator.clipboard.writeText`，在 `file://`
   或 http 内网下该 API 不可用，点了没反应也不报错。已补 `execCommand` 兜底，再失败退化为 `window.prompt`。
3. **`scrollTo` 未兜底导致整树崩溃（我引入的）**：详情区滚回顶部原写成 `panel?.scrollTo({...})`，
   在缺少该方法的环境抛错且未捕获，会让 React 卸载整棵树直接白屏。jsdom 冒烟第一时间炸了出来，
   已加类型判断与 `scrollTop` 回退。
4. **份量在切换酒时丢失**：`servings` 原存在 `RecipePanel` 内部并被 `key={selected.id}` 重置，
   连看几杯就要反复点加号。提升到 shell 状态并持久化（属行为改进，原逻辑本身不算错）。
5. **未使用的样式 token**：`--shadow-panel` 在 `styles.css` 声明后全站零引用，已删；
   另有 `gates.tsx` 里一处 `dark:bg-white/20` 随 auth 层一起移除（全站只有深色配色，该分支永不生效）。

## 三、体验与视觉

### 新增「我的吧台」

侧栏新入口。勾选家里已有的材料（52 种，常用置顶、已选置顶、可按名称筛），
酒单按「现在就能调 / 只缺 1 样 / 只缺 2 样…」分组，并可设「最多缺 N 样」阈值。
卡片上直接写明还缺哪几样；详情页里缺的材料置灰划线，齐了给「吧台材料齐了」徽标。
配料名做了同义归一（`伦敦干金酒→金酒`、`银龙舌兰→龙舌兰`、`陈年/深色/牙买加朗姆→黑朗姆`、
`苏打水→气泡水`、`橙汁→新鲜橙汁`、`蛋白或 aquafaba→蛋白` 等，见 `INGREDIENT_ALIAS`），
避免同一种原料因叫法不同而漏匹配。
**注意**：`苦艾酒`（absinthe）与 `干/甜苦艾酒`（vermouth）是两种原料，刻意不合并。
苦精、盐、伍斯特酱等调味/装饰料不进库存清单，也不计入缺口；`optional: true` 的材料同样不算缺口。

### 数据补全

50 杯酒全部补 `abv`（成品近似酒精度）与 `sweet`（甜度 1~5），
用于详情页「度数/甜度」规格块、列表卡片的度数档位标签，以及新增的排序维度。

### 搜索与排序

搜索命中处高亮；`/` 聚焦搜索、`↑` `↓` 在酒单间移动、`Enter` 打开、`Esc` 关闭；
空态文案按当前视图区分（吧台 / 收藏 / 大类各说各的话）。
新增排序：默认 / 度数高→低 / 度数低→高 / 偏甜→偏干 / 省时优先。

### 配方页

- ml ↔ oz 切换（按 ¼ oz 刻度取整：7.5→¼、22.5→¾；不足 ¼ oz 回退 ml）
- 份量 1~6 杯，跨酒保留、刷新保留
- 步骤序号可点击打勾，方便边做边对进度，可重置
- 新增「复制这杯酒的链接」，配合 hash 深链（`#/gin/negroni`）刷新与分享都能还原视图
- 切大类时酒单回到顶部，换酒时详情区回到顶部
- 「今日特调」卡片从「全部」视图提到所有视图

### 视觉与无障碍

- 列表杯型里的酒液按大类着色（新增 `CATEGORY_COLOR`），卡片左侧加同色细条，侧栏大类方块同步取色
- 收藏按钮下沉到列表卡片右上角（hover / 键盘聚焦显形），不必进详情
- 加「跳到酒单」skip link、统一 `:focus-visible` 轮廓、补齐 `aria-pressed` / `aria-current` / `aria-label`
- 移动端详情区底部用 `env(safe-area-inset-bottom)` 避让手势条
- `prefers-reduced-motion` 下把原本仅 hover 显示的指示符常驻，避免关掉动效后丢线索
- 字体（Noto Sans/Serif SC）从模板注入改回 `index.html` 里的 Google Fonts 直连，删掉品牌探测脚本
- favicon 内联为 data URI，分享图 `og:image` 与 apple-touch-icon 补进 head

## 四、验证

沙箱内执行的检查（无浏览器，故用 jsdom 跑**生产构建产物**，不是源码）：

- `tsc --noEmit`：0 错误（并新增 `noUnusedLocals` / `noUnusedParameters`）
- `eslint .`：0 错误 0 警告
- `vite build`：成功；`index.js` 285.08 KB（gzip 89.60 KB）、`index.css` 29.70 KB（gzip 6.78 KB）
- 纯函数用例：别名归一、absinthe 与 vermouth 不合并、ml→oz 换算、份数叠加、
  空库存/别名库存的吧台匹配、排序单调性、50 杯酒字段完整性 —— 全通过
- jsdom 冒烟 16 项：首屏渲染 43 KB、深链选中内格罗尼、吧台勾选、
  勾「金酒+汤力水」命中「现在就能调 · 金汤力」等 —— 全通过（过程中捕获上面第 3 条崩溃）
- `vite dev` 本地起服：HTTP 200，模块正常编译
- **数据无损比对**：把 50 杯酒逐条与原版 diff，确认除新增的 `abv`/`sweet` 两字段与
  17 处刻意的配料改名外，其余字段（材料/步骤/装饰/备注）与原版逐字节一致；
  `CATEGORIES`、`GLASS_LABEL`、`METHOD_LABEL` 未改动

### 新增数据自检脚本

`scripts/verify-data.mjs`（`npm run verify`，已挂在 `npm run build` 最前面）：
校验 abv/sweet 取值范围、大类合法、id 唯一、别名表自洽（含禁止链式别名）、
absinthe 与 vermouth 不得合并、盎司换算不退化成 NaN、经典组合的吧台匹配、
库存清单不混入调料装饰、排序单调。以后你直接编辑 `cocktails.ts` 加酒，跑一下就能兜住低级错误。
为此把 `INGREDIENT_ALIAS` 改为具名导出。

## 五、没做的事

- **未扩充酒谱数量**（仍是 50 杯）：本次方向不含数据补全，且批量加酒需逐条核对配比。
- **未做浅色主题**：全站配色是深色酒馆设计，反色工作量不小、收益一般。
- **手机详情仍是整屏替换 + 返回箭头**（沿用原交互），没改成底部抽屉：当前实现已可用，
  且少一套手势状态。想要真抽屉我再单独加。
- 真机 iOS Safari / Android Chrome 的触控与键盘导航回归，需要你本地跑一次确认。
